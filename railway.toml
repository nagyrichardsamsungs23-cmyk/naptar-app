[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py seed_data && gunicorn config.wsgi --bind 0.0.0.0:${PORT:-8000} --workers 2"

[service]
healthcheckPath = "/login/"
